# Server
server.port=8080

# H2 In-Memory DB (dev/default)
spring.datasource.url=jdbc:h2:mem:movieapp;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.settings.web-allow-others=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL (commented; activate with spring.profiles.active=prod)
# spring.datasource.url=jdbc:postgresql://localhost:5432/movieapp
# spring.datasource.username=user
# spring.datasource.password=pass
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update

# App Name (optional, good for Actuator/info)
spring.application.name=movie-booking-monolith

# Logging
logging.level.com.moviebooking=DEBUG
logging.level.org.springframework.web=DEBUG

# DevTools
spring.devtools.restart.enabled=true
management.endpoints.web.exposure.include=health,info
